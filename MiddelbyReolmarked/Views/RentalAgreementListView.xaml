<UserControl x:Class="MiddelbyReolmarked.Views.RentalAgreementListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Error message -->
        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,0,0,10" Grid.Row="0"/>

        <!-- Rental agreements list -->
        <DataGrid ItemsSource="{Binding RentalAgreements}" 
                  SelectedItem="{Binding SelectedRentalAgreement, Mode=TwoWay}" 
                  AutoGenerateColumns="False" 
                  Grid.Row="1"
                  Margin="0,0,0,10"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding RentalAgreementId}" Width="Auto"/>
                <DataGridTextColumn Header="Kunde ID" Binding="{Binding CustomerId}" Width="Auto"/>
                <DataGridTextColumn Header="Reol ID" Binding="{Binding RackId}" Width="Auto"/>
                <DataGridTextColumn Header="Startdato" Binding="{Binding StartDate, StringFormat=d}" Width="Auto"/>
                <DataGridTextColumn Header="Slutdato" Binding="{Binding EndDate, StringFormat=d}" Width="Auto"/>
                <DataGridTextColumn Header="Status ID" Binding="{Binding RentalStatusId}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Actions -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Left" Margin="0,10,0,0">
            <Button Content="Opret ny lejeaftale" Width="140" Margin="0,0,10,0" Command="{Binding ShowRentalAgreementViewCommand}"/>
            <Button Content="Afslut lejeaftale" Width="140" Margin="0,0,10,0"/>
            <!-- Tilfoej Command bindings og evt. inputfelter efter behov -->
        </StackPanel>
    </Grid>
</UserControl>