<UserControl x:Class="MiddelbyReolmarked.Views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid Margin="20" Background="Beige">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Overskrift -->
            <RowDefinition Height="Auto"/>
            <!-- Navn -->
            <RowDefinition Height="Auto"/>
            <!-- Telefon -->
            <RowDefinition Height="Auto"/>
            <!-- E-mail -->
            <RowDefinition Height="Auto"/>
            <!-- Actions -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <!-- Overskrift -->
        <TextBlock Text="Kundeoplysninger" FontSize="20" FontWeight="Bold" Grid.Row="0" Grid.ColumnSpan="3" Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <!-- Navn -->
        <Label Content="Navn:" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="0,5" Width="100"/>
        <TextBox Grid.Row="1" Grid.Column="2" Margin="5,5" Width="200"/>

        <!-- Telefon -->
        <Label Content="Telefon:" Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" Margin="0,5" Width="100"/>
        <TextBox Grid.Row="2" Grid.Column="2" Margin="5,5" Width="200"/>

        <!-- E-mail -->
        <Label Content="E-mail:" Grid.Row="3" Grid.Column="1" VerticalAlignment="Center" Margin="0,5" Width="100"/>
        <TextBox Grid.Row="3" Grid.Column="2" Margin="5,5" Width="200"/>

        <!-- Actions -->
        <StackPanel Orientation="Horizontal" Grid.Row="4" Grid.ColumnSpan="3" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Gem" Width="90" Height="26" Margin="10,0"/>
            <Button Content="Slet" Width="90" Height="26" Margin="10,0"/>
        </StackPanel>

    </Grid>
</UserControl>
